CREATE SEQUENCE IF NOT EXISTS app_users_seq START WITH 1 INCREMENT BY 50;

CREATE SEQUENCE IF NOT EXISTS review_seq START WITH 1 INCREMENT BY 50;

CREATE TABLE app_users
(
    id       BIGINT       NOT NULL,
    username VARCHAR(255) NOT NULL,
    email    VARCHAR(255) NOT NULL,
    CONSTRAINT pk_app_users PRIMARY KEY (id)
);

CREATE TABLE bean
(
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name        VARCHAR(255),
    description VARCHAR(255),
    CONSTRAINT pk_bean PRIMARY KEY (id)
);

CREATE TABLE review
(
    id      BIGINT  NOT NULL,
    rating  INTEGER NOT NULL,
    comment VARCHAR(255),
    user_id BIGINT,
    bean_id BIGINT,
    CONSTRAINT pk_review PRIMARY KEY (id)
);

ALTER TABLE app_users
    ADD CONSTRAINT uc_app_users_email UNIQUE (email);

ALTER TABLE app_users
    ADD CONSTRAINT uc_app_users_username UNIQUE (username);

ALTER TABLE review
    ADD CONSTRAINT FK_REVIEW_ON_BEAN FOREIGN KEY (bean_id) REFERENCES bean (id);

ALTER TABLE review
    ADD CONSTRAINT FK_REVIEW_ON_USER FOREIGN KEY (user_id) REFERENCES app_users (id);